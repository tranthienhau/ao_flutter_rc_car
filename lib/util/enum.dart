enum CarState { empty, atomic, combine }

enum Direction { forward, backward, left, right }

enum Action {
  goDown,
  goDownLeft,
  goLeft,
  goUpLeft,
  goUp,
  goUpRight,
  goRight,
  goDownRight,
  stop
}
